import React from 'react';

const tasksData = React.createContext({
  // dataRequestHandler: () => {},
  // tasksData: false,
  // dataRequestDetails: '',
  // resetHandlerChoice: () => {}
});

export default tasksData;
